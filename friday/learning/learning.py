\"\"\"Learning facade for usage tracking and improvement.\"\"\"\n\nfrom .logger import log_decision, update_outcome\nfrom .trainer import retrain_model\nfrom .metrics import show_stats as print_stats\n\nclass LearningSystem:\n    def __init__(self):\n        self.usage_count = 0\n\n    def track_usage(self, action: str):\n        self.usage_count += 1\n        log_decision('SYSTEM', action, 1.0, 1.0)  # Log system actions\n\n    def improve(self):\n        retrain_model()\n        print_stats()\n\n    def log_trade(self, symbol: str, decision: str, pred_return: float, conf: float):\n        tid = log_decision(symbol, decision, pred_return, conf)\n        # Simulate update after time\n        from evaluator import evaluate_trade\n        evaluate_trade(tid, pred_return * 1.1)  # Sim profit\n\nlearning = LearningSystem()
